import { useContext } from "react";
import { BoxesContext } from "../context/boxesContext";

const useBoxes = () => {

    const {  } = useContext(BoxesContext)

    return {
        
    };
}
 
export default useBoxes;